import { Paper, Typography } from '@mui/material';

export const FavoritesPage = () => (
  <Paper sx={{ p: 3 }}>
    <Typography variant="h5">Избранные вакансии</Typography>
    <Typography color="text.secondary">Заглушка с легко заменяемым data-source.</Typography>
  </Paper>
);
